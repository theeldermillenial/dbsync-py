# Sample environment configuration for dbsync-py
# Copy this file to .env and modify the values as needed

# Option 1: Complete database URL (takes precedence over individual variables)
# DBSYNC_DATABASE_URL=postgresql://username:password@localhost:5432/cexplorer

# Option 2: Individual database connection parameters
DBSYNC_HOST=localhost
DBSYNC_PORT=5432
DBSYNC_DB_NAME=cexplorer
DBSYNC_USER=your_username
DBSYNC_PASS=your_password

# Additional examples:
# For remote database:
# DBSYNC_HOST=your-db-server.com
# DBSYNC_PORT=5432

# For local development with different database name:
# DBSYNC_DB_NAME=cardano_testnet

# For production with full URL:
# DBSYNC_DATABASE_URL=postgresql://cardano_user:secure_password@db.example.com:5432/mainnet_cexplorer
